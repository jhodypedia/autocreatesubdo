<!doctype html>
<html>
<head><%- include('partials/head', { title: 'Create Subdomain' }) %></head>
<body>
  <%- include('partials/nav') %>
  <main class="container py-4">
    <% if (res && res.locals && res.locals.flash) { %>
      <script>
        Swal.fire({ icon: '<%= res.locals.flash.type === "success" ? "success" : "error" %>', title: "<%= res.locals.flash.msg %>", timer: 3000 });
      </script>
    <% } %>

    <div class="row">
      <div class="col-lg-7">
        <div class="card shadow-sm mb-3 hover-zoom">
          <div class="card-body">
            <h4>Create Subdomain on <strong><%= domain %></strong></h4>
            <form id="createForm" method="POST" action="/create">
              <div class="mb-3">
                <label class="form-label">Subdomain</label>
                <input class="form-control" name="name" placeholder="example" required maxlength="63">
              </div>
              <div class="mb-3">
                <label class="form-label">Target (IPv4 or hostname)</label>
                <input class="form-control" name="target" placeholder="203.0.113.55 or server.example.net" required>
              </div>

              <% if (turnstileSite) { %>
                <div class="cf-turnstile" data-sitekey="<%= turnstileSite %>"></div>
                <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
              <% } %>

              <div class="d-flex gap-2 mt-3">
                <button class="btn btn-success" type="submit"><i class="fa-solid fa-plus"></i> Create</button>
                <a class="btn btn-outline-secondary" href="/list"><i class="fa-solid fa-list"></i> View List</a>
              </div>
            </form>
          </div>
        </div>

        <div class="card shadow-sm mb-3">
          <div class="card-body">
            <h5 class="mb-3">How it works</h5>
            <ol>
              <li>Enter a subdomain name (a-z0-9- only) and a target IP or hostname.</li>
              <li>We create DNS record on Cloudflare (A or CNAME).</li>
              <li>Owner is automatically detected from your IP address (no login required).</li>
            </ol>
          </div>
        </div>
      </div>

      <div class="col-lg-5">
        <div class="card shadow-sm mb-3">
          <div class="card-body text-center">
            <h5>Sponsored</h5>
            <div class="ad-wrap mt-3">
              <!-- Adsterra script placeholder -->
              <%- adScript || '<div class=\"p-3 border rounded\">Place Adsterra script in Admin Settings</div>' %>
            </div>
          </div>
        </div>

        <div class="card shadow-sm">
          <div class="card-body">
            <h6>Notes</h6>
            <ul>
              <li>Daily limit per IP is configurable in Admin.</li>
              <li>Reserved names are blocked.</li>
              <li>Admins can manage domain and ad settings.</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="text-center py-3 text-muted small">© <%= new Date().getFullYear() %> SubManager • <%= domain %></footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/app.js"></script>
</body>
</html>
